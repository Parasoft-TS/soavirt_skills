# Decision Log

Purpose: capture major workflow and technical decisions with rationale.

| Date | Decision | Rationale | Impact |
|---|---|---|---|
| 2026-03-16 | Treat API-returned runtime ids as authoritative normalized ids, treat tool-produced custom columns as valid "data source columns," and document request-payload parameterization at primitive leaves only. | Contributor follow-up on the Parasoft Demo App Data Generator trial exposed three reusable ambiguity points at once: runtime ids returned by the API were normalized under `/TestAssets/...` instead of the local repo-root-prefixed path, SOAtest/Virtualize overloaded "data source column" to include Data Bank/Data Generator custom-column outputs, and the validated downstream binding pattern parameterized primitive leaf values inside request payloads rather than replacing whole objects or subtrees. Capturing the policy together makes future authoring more predictable. | Future agents should trust API-returned ids when targeting runtime objects, should not prepend local workspace roots to those ids, may treat `customColumn.customColumnName` outputs as valid downstream data-source columns, and should parameterize request bodies by placing `${TOKEN}` at primitive leaf values while keeping complex-object or whole-subtree substitution out of scope unless a future validation effort proves it. |
| 2026-03-16 | Make experimental validator message mapping deterministic, freeze Skill 065 coverage denominators before breadth accounting, tighten authoring-integrity assertor guidance, and codify first-pass negative expected-code calibration. | Follow-up on the ParasoftDemoApp experimental run showed that the biggest remaining gaps were no longer high-level phase integrity but lower-level execution ambiguity: validator message paths could still be guessed semantically, negative/security breadth accounting still depended too much on implicit mental inventories, narrow authoring-integrity assertors could drift into weak wrapper-level `hasContent` checks, and negative branches benefited from an explicit one-pass calibration pattern before locking exact expected codes. | Future agents should derive OpenAPI validator message mapping from exact authored producer identity plus the confirmed operation template, should freeze and refresh an explicit happy-path-derived denominator for negative/security accounting, should prefer semantically meaningful authoring-integrity assertor targets over broad wrapper presence checks, and may use one focused temporary-range calibration run before tightening negative expected-code expectations to the exact observed behavior. |
| 2026-03-16 | Keep Skill 065 as the primary normative owner for phase-eligibility, next-safe-action, and blocker-aggregation behavior, with workflow and templates limited to reusable guardrails plus output-contract enforcement. | Contributor follow-up clarified that the main risk was not only policy wording but ownership drift: if the behavioral expectations live mostly in the workflow doc or templates, future agents may still miss how the experimental lane should continue slice-by-slice. The clearer model is a thin global runtime rule, primary execution semantics in Skill 065, and templates that only enforce how those results are reported. | Future agents should look to Skill 065 first for the concrete experimental-lane continuation/stop rules, should use the workflow doc only for the reusable required-phase guardrail, and should treat the templates as reporting contracts rather than as the main source of execution behavior. |
| 2026-03-16 | Distinguish phase-pending required work from true terminal blockers in runtime-global policy and Skill 065 completion semantics. | Contributor review of the interrupted experimental run showed the recent no-shortcut hardening solved the shallow-completion problem but still left a semantic gap: downstream required work that was merely pending behind unfinished happy-path authoring could be rendered as if it were already terminally `blocked`/`deferred`. The safer repair is to keep fail-closed required-phase integrity while making terminal `partial`/`blocked` outcomes contingent on reasonable continuation and exhaustion of the next safe in-scope action. | Future agents should keep required downstream phases visible without converting not-yet-reached slices into terminal blocker states, should continue autonomous Skill 065 execution while any next deterministic in-scope action remains, and should reserve final `partial`/`blocked` reporting for true stop conditions with exact unfinished-phase reasons. |
| 2026-03-16 | Make Skill 065 approval/completion templates expose required-phase status and per-slice/per-operation accounting explicitly. | Contributor discussion showed that even when Skill 065 card text became stricter, the approval and completion artifacts still allowed broad prose summaries that could hide missing negative/security/validation follow-through or imply unnecessary negative-suite hierarchy. The safer repair is to make the templates themselves require required-phase visibility plus per-slice/per-operation accounting. | Future agents should show required in-scope phases explicitly in the Skill 065 approval/completion artifacts, should keep negative/security accounting visible per eligible slice or operation instead of broad representative summaries, and should reflect the flatter single-step negative structure directly in the approval surface. |
| 2026-03-16 | Promote required-phase completion integrity to runtime-global policy instead of keeping no-shortcut discipline only inside one orchestration card. | Contributor discussion showed that the underlying risk is broader than autonomous Skill 065: any workflow with mandatory phases can drift by treating early success or partial progress as permission to stop. A global phase-integrity rule is clearer and less personality-coded than trying to encode a "don't be lazy" policy inside one card. | Future agents should complete every in-scope required phase unless the user narrows scope or the canonical card marks the phase optional/deferred; if a later phase cannot be finished credibly, they should stop as `partial` or `blocked` and name the unfinished phase rather than reporting effective completion. |
| 2026-03-16 | Flatten single-step negative coverage in experimental Skill 065 and reserve extra hierarchy for lifecycle-dependent negative branches only. | Contributor review of the latest Skill 065 output showed the negative tree was accumulating unnecessary bundle and `Negative Cases` wrappers around one-client negatives, which increases authoring cost and obscures operation-level breadth. The cleaner rule is to keep single-step negatives as directly named operation-oriented tests under `Negative Coverage`, and use grouped bundles only when prerequisite or lifecycle sequencing genuinely requires them. | Future agents should place single-step negative REST Clients directly under `Negative Coverage` with deterministic operation-plus-case naming, should create grouped negative bundles only for `minimal-prefix` or `full-sequence` cases, and should expose negative accounting per eligible slice clearly in completion reporting. |
| 2026-03-16 | Strip inherited happy-path enrichment from copied experimental negative/security branches before authoring branch-local tooling. | Contributor review of the latest Skill 065 run showed that generic tool copy can carry happy-path validators, diff/assertors, and databanks into derived negative/security branches, which contaminates those branches and creates misleading failures. The intended model is to reuse same-host request/auth/state posture, not to inherit happy-path enrichment blindly. | Future agents should inspect copied negative/security producers immediately, remove inherited validators/assertors/diff tools and any producer-local databanks that are not explicitly needed by the derived branch, then attach only the branch-local tools justified for that negative or security branch. |
| 2026-03-16 | Treat correctly configured experimental validator failures as valid findings, and require happy-path validation bundles to include schema validation plus content validation by default. | Contributor review of the latest Skill 065 run showed two opposing drifts: validators were sometimes treated as expendable when they reported real schema defects, and happy-path validation could stop after attaching only validators because finishing the full bundle felt expensive. The intended repair is to keep correctly configured failing validators as evidence of service defects, while also making the default happy-path enrichment target a validator plus assertor/diff bundle rather than validator-only attachment. | Future agents should triage validator failures for misconfiguration versus genuine contract defects, report genuine defects as delivered findings instead of deleting the validator, and complete non-empty JSON/XML happy-path validation with both schema validation and content validation unless the slice is explicitly blocked or a no-tool exception. |
| 2026-03-16 | Encode no-shortcut phase discipline in experimental Skill 065 autonomous runs. | Contributor review showed that broad autonomous runs may feel large enough that an agent implicitly narrows scope once a host `.tst` executes or once one major phase is complete. The intended model is stricter: all in-scope required phases remain mandatory unless the user narrows scope or the card explicitly defers them. | Future agents should not use task size or uncertainty as permission to skip negative, security, validation, or verification phases; if a later phase cannot be completed credibly, they should stop as partial or blocked and name the exact unfinished phase instead of claiming effective completion. |
| 2026-03-16 | Keep other `.tst` lookup reference-only inside experimental Skill 065, and require all authored population to come from the current host asset's disabled generated template suite or same-host authored branches. | Contributor review of a failed autonomous run showed that an agent could inspect another experimental `.tst` in the project and then shortcut Skill 065 by copying authored suites or tools into a fresh host asset. The intended model is narrower: foreign `.tst` assets may help with orientation or troubleshooting, but the new host `.tst` must still be built by following Skill 065's own template-plus-ledger logic. | Future agents may inspect other `.tst` assets for reference-only context, but must not copy authored suites/tests/tools from them into the new host asset; happy-path REST Client copies must come from the disabled generated template suite in the current host `.tst`, and negative/security branches must derive from already-authored copies in that same host asset. |
| 2026-03-16 | Delete empty experimental Skill 065 happy-path child suites at the end of authoring instead of preserving structural placeholders. | Contributor review of the latest experimental operator run showed that some clusters legitimately had no viable content for one of the canonical happy-path children, such as an empty `Lifecycle Flow` under a lookup-only cluster. Preserving those empty suites made the final authored structure look broader than the actual delivered coverage. The cleaner repair is to keep the canonical skeleton during authoring, then run a final cleanup pass that deletes any direct `Happy Path` child suite that remained empty. | Future agents should still create the canonical `Happy Path` skeleton up front, but should delete empty direct children such as unpopulated `Lifecycle Flow`, `Safe Reference Reads`, or `Baseline Snapshot` suites before completion when they ended with no meaningful authored descendants. |
| 2026-03-16 | Keep experimental security coverage broad through Skill 065 per-operation accounting, and let Skill 066 supply advisory exact-operation security-targeting hints with lifecycle-preferred source selection. | Contributor review of the latest experimental operator run showed that the lane was still vulnerable to collapsing security coverage into one representative copied branch per cluster even though the intended structure was broader. The clean repair is to make Skill 065 account explicitly for every eligible authored operation, prefer lifecycle-flow copies of the exact operation when choosing a security seed, fall back to safe-read or baseline copies of that same operation only when needed, and let Skill 066 provide advisory security-targeting hints or narrowly-targeted extra exploration when the existing evidence is too thin. | Future agents should not treat one representative security branch per cluster as sufficient, should keep security accounting operation-level rather than cluster-level, should prefer lifecycle-flow copies of the exact target operation when credible, should record `no-credible-target` or `deferred` instead of widening to a different representative operation, and may use bounded additional exploration only when it materially improves the advisory exact-operation security-targeting signal consumed by Skill 065. |
| 2026-03-16 | Keep experimental negative coverage broad through Skill 065 per-slice accounting, and let Skill 066 supply advisory negative-opportunity hints with bounded extra exploration only when needed. | Contributor review of the latest experimental operator run showed that the lane's fail-closed negative-family heuristics successfully avoided fake negatives, but still let autonomous runs under-deliver breadth and sometimes surface local negative uncertainty as if it were a user-facing blocker. The clean repair is to keep the five-family rule as a late-stage per-slice ceiling, require Skill 065 to account explicitly for every eligible slice, treat weak negative candidates as `no-credible-family` or `deferred` unless a real dependency gap exists, and let Skill 066 provide advisory negative-opportunity hints or narrowly-targeted extra exploration when the existing evidence is too thin. | Future agents should not treat a few strong negatives in one cluster as sufficient for the whole experimental lane, should keep the five-family cap per eligible slice rather than as a run-wide budget, should avoid surfacing ordinary negative-design uncertainty as a blocker in autonomous mode, and may use bounded additional exploration only when it materially improves the advisory negative-opportunity signal consumed by Skill 065. |
| 2026-03-16 | Keep the Skill 066 exploration ledger advisory, and synchronize experimental happy-path validation through a Skill 065-owned handoff packet into Skill 067 with databank-reuse-first enrichment and posture inheritance. | Contributor review showed that the experimental lane already used exploration strongly for authoring, but validation enrichment still behaved like a separate thin-input workflow with its own approval model and weak awareness of clustered cross-step bindings. The cleaner repair is not to harden Skill 066 into a rigid validation contract, but to make Skill 065 emit an explicit validation handoff packet per authored slice, let Skill 067 treat exploration evidence as baseline authority for family selection and candidate expected content, keep schema-validator source confirmation fail-closed, and require validation-only extracts to reuse an existing producer-local databank before creating a sibling databank. | Future agents should keep the Skill 066 ledger advisory, should make happy-path validation enrichment a required experimental-lane phase, should let Skill 065 own the handoff into Skill 067, should let Skill 067 auto-add cross-step semantic validation only when provenance/relationship/volatility/scope checks are strong enough, should preserve autonomous posture without a separate validation approval artifact by default, and should reuse/update an existing producer-local databank before creating a second chained databank under the same producer. |
| 2026-03-16 | Make post-generation `BASEURL` normalization of generated OpenAPI template suites a Skill 065-owned orchestration step, with Skill 064 owning generated-local-environment variable mechanics and Skill 020 owning in-place REST Client URL rewrite mechanics. | Runtime contributor review showed that the generated OpenAPI template suite still carried concrete host URLs even though Skill 065 later copies those REST Clients into many authored branches. The durable fix is to normalize once immediately after Skill 022 create/readback by adding a local `BASEURL` variable and rewriting only the generated template-suite REST Clients whose URLs safely decompose into that confirmed base plus relative remainder. This keeps Skill 022 scoped to initial generation/create-readback, preserves Skill 064 as the environment-mechanics owner, and uses Skill 020's existing read-merge-write REST Client mutation contract for the actual URL rewrite. | Future agents should, in Skill 065's broad new-file lane, insert/readback-confirm a generated-local `BASEURL` variable before freezing the generated suite as source-of-copy, rewrite matching generated REST Client URLs to `${BASEURL}` plus relative path/query, preserve already-parameterized or non-matching URLs as-is, and keep Skill 022 generation-only rather than absorbing caller-specific normalization. |
| 2026-03-16 | Canonicalize the experimental Skill 065 lane around cluster-local authored structure, a stateful Skill 066 ledger, and Skill 074 disable-run-restore verification sandboxing. | Contributor planning for the experimental lane showed that the original per-operation exploration bundle plus generated-client mutation flow was not strong enough to preserve stateful sequencing, branch isolation, or consistent `.tst` structure. The stable repair is to make Skill 066 emit a resource/workflow-cluster overlay with ordered authoring paths, make Skill 065 preserve the generated OpenAPI suite as a disabled template/source-of-copy while authoring a sibling `AI Generated Tests` subtree, keep non-restoring but credible mutable flows in normal happy-path coverage, and treat Skill 074 as the default focused-verification mechanism instead of relying primarily on coarse `testNames` filtering. | Future agents should implement the explicit experimental lane as cluster-oriented rather than flat per-operation CRUD authoring, should preserve the generated suite as disabled source-of-copy, should build cluster-local `Happy Path` / narrow `Optional / Deferred` / `Negative Coverage` / `Security Coverage` structure under `AI Generated Tests`, should derive negatives and security from configured happy-path copies using the documented trigger rules, and should prefer Skill 074 disable-run-restore sandboxing over `testNames` for focused verification. |
| 2026-03-15 | Split durable secret-reference storage from runtime auth materialization, keeping gitignored secret refs for bootstrap while allowing supported SOAtest auth write branches to resolve and materialize plaintext credentials into `.tst` assets. | Contributor review of the experimental lane showed that the current docs correctly encouraged gitignored secret-reference storage at bootstrap time but left write-time auth behavior underspecified, which pushed execution into an out-of-scope environment/reference-indirection workaround. The intended boundary is narrower: secret-reference storage is the bootstrap concern, while live exploration and supported REST Client auth mutation may consume those referenced secrets directly, and post-generation source-control protection remains the user's responsibility. | Future agents should store only secret references in project context, may resolve approved gitignored secret refs for exploration/execution and supported Basic-auth writes, should let Skills 020/059 materialize those credentials directly into the `.tst` when required, and should not classify such branches as blocked or reroute them into environment/reference concealment workarounds unless the user explicitly asks for that. |
| 2026-03-15 | Treat OpenAPI `.tst` generation as producing the standard unconstrained generated REST Client branch, and keep Skill 065's broad new-file handoff on Skill 020 rather than Skill 059. | Contributor review of experimental-lane behavior showed that Skill 065 still left room to infer that `Create TST from OpenAPI` might yield constrained REST Clients because it inspected the post-generation client family and listed both Skills 020 and 059 as normal generated-branch mutation leaves. The intended model is narrower: broad OpenAPI `.tst` generation yields the standard generated unconstrained branch, while constrained REST Client ownership remains primarily for refactor, existing constrained edits, and explicit direct constrained-client requests. | Future agents should treat Skill 022 generation output as the standard unconstrained / None-mode generated branch, should route Skill 065's post-generation generated-client request/auth mutation through Skill 020, and should stop inferring constrained REST Client creation as the normal broad OpenAPI-generation outcome unless a later validated branch explicitly says otherwise. |
| 2026-03-15 | Classify `AGENTS.md` startup into contributor-start vs operator/project-start before auto-entering Skill 063, with bare `Load AGENTS.md` remaining the operator default. | README guidance and contributor feedback distinguish explicit skill-contributor bootstrap from project-aware runtime startup, but the recent all-`AGENTS.md` rule collapsed both paths into mandatory project bootstrap. The cleaner repair is to make startup intent classification the canonical gate and keep Skill 063 focused on project context rather than contributor-session rejection. | Future agents should skip automatic Skill 063 when the startup prompt explicitly requests skill contributor/skill authoring workflow, should still auto-run Skill 063 for explicit project/operator starts and bare under-specified `Load AGENTS.md` prompts, and should ask one clarification question only when contributor and project/runtime cues conflict. |
| 2026-03-15 | Split API-preferred mutation ownership into class-specific rename, generic tool lifecycle, and broader disabled-state lanes, and treat local YAML editing as explicit exception-only helper behavior. | Contributor review plus cached OpenAPI comparison showed generic `/v6/tools` owns only `disabled`, while class-specific tool `PUT` schemas consistently own `name` plus class payload, and `/v6/tools/disabled` spans more than tools alone. That made ordinary rename the primary universal class-specific gap, while pure tool copy/move/delete and supported enable/disable needed separate operator-facing owners. | Future agents should route pure rename requests to the new atomic rename owner, pure tool copy/move/delete requests to the generic tool lifecycle leaves, and supported enable/disable requests to the broader disabled-state family; Skill 006 and other YAML-authorized cards must not be used as substitutes for ordinary lifecycle/configuration mutation when supported API write paths exist. |
| 2026-03-15 | Treat REST Client Basic auth as owned by the first-class `httpAuthentication` API subtree rather than by default literal `Authorization` header injection. | Live contributor investigation proved that existing REST Clients can be updated safely through `GET/PUT/GET` on `httpOptions.transport.<http10|http11>.security.httpAuthentication.performAuthentication`, including the Basic username/password wrapper shapes and stable readback of the persisted auth subtree. The prior docs left this capability undocumented, which encouraged orchestration layers to misclassify generated clients as auth-blocked. | Future agents should prefer the native Basic-auth model in Skills `020` and `059` when credentials are available, should treat readback confirmation of the persisted auth subtree as sufficient configuration proof unless runtime verification is separately requested by the caller or owning workflow, should reserve literal `Authorization` headers for unsupported/custom schemes such as future OAuth2 work, and should stop rather than guess for unvalidated auth types or unresolved transport branches. |
| 2026-03-15 | Treat loading `AGENTS.md` as explicit opt-in to SOAtest/Virtualize-oriented follow-on work, and require immediate Skill 063 project bootstrap before any further task work. | A drift regression reintroduced wording that made Skill 063 conditional on later project-aware work, which allowed agents to stop after loading startup docs instead of immediately resolving project context. The intended contract is stronger: once `AGENTS.md` is loaded, the session has already entered the SOAtest/Virtualize runtime lane, so project bootstrap is part of startup itself rather than a later optional branch. | Future agents should invoke Skill 063 immediately after the required startup reading whenever `AGENTS.md` has been loaded, should not defer bootstrap because the next task appears contributor-facing or not yet project-specific, and should preserve that same expectation after startup re-intake in resumed sessions. |
| 2026-03-15 | Add an explicit opt-in experimental exploration-first broad-authoring lane via Skills `065-067`, while preserving stable `033/057/058` as the default production path. | Contributor planning showed that the desired optimization depends on a different evidence model: bounded direct endpoint exploration should supply the baseline for readiness and validation design, but the stable cards intentionally prohibit that behavior. An additive lane keeps the validated execution-backed path intact while giving the experiment clear ownership boundaries, routing rules, and rollback semantics. | Future agents should route exploration-backed broad authoring only when the user explicitly opts in, should treat Skills `065/066/067` as the sole owners of the experimental evidence model, and should keep stable `033/057/058` unchanged as the default route until a later migration decision is made. |
| 2026-03-14 | Canonicalize environment handling around one dedicated environment owner, keep Skill 063 limited to semantic/bootstrap ownership, and treat referenced-environment attachment as structurally valid but not automatic proof of runtime activation. | Drift-hardening review showed the repo was overloading the word `environment` across semantic project environments, external project `.env` / `.envs` files, internal SOAtest Environment objects, referenced environment nodes, and active execution-time state. The validated WSDL-generation evidence also showed that creating a referenced environment node does not by itself move runtime authority away from the currently active local environment. | Future agents should use Skill 063 for semantic project environment capture and durable environment-file references, should use the dedicated environment owner for internal/reference/active-environment mechanics, and must verify active-environment behavior explicitly before assuming runtime has switched to an external environment file. |
| 2026-03-14 | Close out the merged local-workspace architecture rewrite as fully implemented repo-wide, while intentionally retaining Skills `002-005` on disk only as non-routing deferred legacy records. | After the second-wave rewrites and the legacy-card de-routing pass, the remaining follow-up was a repo-wide sweep for stale wording and a final disposition for `002-005`. The sweep found only a few stray phrases to normalize, and the preferred outcome was to keep the old cards as historical compatibility records without leaving them visible in operator routing. | Future contributors should treat the architecture rewrite as implemented, should not reopen routing around `002-005`, and should view any future decision to delete those cards as optional cleanup rather than unfinished architectural migration work. |
| 2026-03-14 | Remove Skills `002-005` from `skill-index.md` entirely and reduce them to deferred legacy compatibility/reference cards instead of keeping them visible as secondary operator routes. | Once the operator-facing cards had been adapted to the merged local-workspace model, leaving `002-005` visible in `skill-index.md` still created an avoidable temptation for future agents to route through the old server-era paths. Keeping the cards only as narrow compatibility/reference artifacts preserves historical endpoint notes without treating them as part of the live routing surface. | Future agents should not encounter Skills `002-005` through the normal routing index, should prefer the local-first platform family and hybrid cards, and should read `002-005` only if an explicit legacy compatibility branch is intentionally invoked. |
| 2026-03-14 | Complete the second architecture-rewrite wave by classifying the remaining affected cards as either local-first YAML analysis or hybrid local-file-plus-API workflows, and remove remaining operator-facing dependence on the legacy server-era `002-005` cards. | After the first-wave rewrite, the remaining drift was concentrated in five cards: two read-only YAML analysis cards still assumed server download/introspection, one structure card still assumed REST-first movement, and the constrained REST refactor cards still inherited server-era transfer/copy assumptions through their dependency chain. The cleanest repair was to classify each card by the new architecture's authority split and rewrite them to follow that lane explicitly instead of preserving transitional wording. | Future agents should treat Skills 007 and 052 as local-path-authoritative YAML analysis cards, treat Skills 008/059/060/061 as hybrid cards that anchor on local `.tst` files and enter Skill 050 only when runtime ids or API-normalized mutation are truly needed, and stop treating `002-005` as required operator-facing dependencies for those workflows. |
| 2026-03-14 | Adopt the merged local-workspace model as the canonical repository architecture: local paths are authoritative for file-backed asset identity, while the localhost API remains authoritative for runtime object ids, semantic mutation, execution, and diagnostics. | Contributor planning for the next architecture wave clarified that this repo is not a remote-server-plus-local-cache workflow; the local workspace and localhost runtime are the permanent baseline for this skills project. Keeping file identity and runtime identity split prevents API-first misrouting for local assets without weakening the cases where the API is still the real source of truth. | Future agents should resolve `.tst`, `.pva`, `.pvn`, env files, and other file-backed assets locally first, should use the API only when runtime-object ids or API-mediated semantics are needed, and should fail closed when resolution stays ambiguous after the allowed routing flow. |
| 2026-03-14 | Move durable application project context from `docs/projects/` to `TestAssets/` and limit Skill 063 to application-under-test bootstrap rather than contributor-repo storage. | Once the merged-workspace model became canonical, keeping reusable application project state under `docs/projects/` no longer matched where the assets themselves live, and the temporary contributor-maintenance branch in Skill 063 became an awkward crossover between operator bootstrap and contributor workflow. Putting durable project state under `TestAssets/<slug>/` keeps bootstrap aligned with the local asset roots while leaving contributor workflow as a separate concern. | Future agents should persist project discovery records in `TestAssets/project-index.yaml`, keep canonical project metadata in `TestAssets/<slug>/<slug>.yaml`, place project-local environment/data/resource files under the project root, use the active project as the default routing anchor for project-local assets, and treat contributor-only doc/policy sessions as outside Skill 063 unless the user explicitly wants project context loaded or stored. |
| 2026-03-14 | Reframe platform file-lifecycle guidance around local-first operations and narrow Skill 050 to API branches, leaving Skills 002-005 as legacy transitional cards pending downstream cleanup. | The old platform layer assumed server-hosted transfer/copy/rename/delete as first-class runtime actions, but the merged-workspace model makes direct local file operations the normal path. The remaining durable guidance is target confirmation, root scoping, rollback-preserving local YAML fallback, and safe API entry only when a branch actually leaves local-only work. | Future agents should treat the platform family plus Skills 001/006 as the primary local-file guidance, should invoke Skill 050 only for API-mediated reads/writes/execution, and should treat Skills 002-005 as temporary compatibility surfaces rather than the preferred operator-facing route. |
| 2026-03-14 | Make Skill 063 service-definition intake environment-aware and require service-definition readback plus BASE_URL confirmation before persisting environment context. | Live contributor review of the new bootstrap flow showed that collecting service-definition locations alone is not enough when several environments exist: future agents could otherwise store incomplete or mis-mapped definition references, or write `BASE_URL` values without first checking whether the definition actually implies that environment-specific URL. Adding an explicit readback-and-confirmation step keeps the common path reliable without forcing a separate specialist workflow. | Future agents should ask the service-definition question in an environment-aware way, should fail closed on ambiguous environment-to-definition mappings, should read provided OpenAPI/WSDL definitions before treating them as understood context, and should confirm the extracted environment-specific `BASE_URL` mapping with the user before writing `BASE_URL` into project environment files. |
| 2026-03-14 | Default Skill 063 new-project intake to application-under-test and reserve the contributor-maintenance repository branch for explicit user-signaled cases. | Live contributor feedback on the first bootstrap run showed that asking every new project whether it was an application-under-test or a contributor-maintenance repository added an unnecessary branch to the common path. The project bootstrap workflow still needs to support contributor-maintenance repository context, but that context should be inferred from explicit user framing rather than introduced as a mandatory classification question in the default interview. | Future agents should skip the app-vs-contributor question during new-project bootstrap, should begin the normal environment-first intake immediately for new projects unless the user explicitly identifies a contributor-maintenance repository context, and should keep the contributor-maintenance branch available only as that explicit alternate path. |
| 2026-03-14 | Require Skill 063 to run immediately after AGENTS/bootstrap required reading for every new session under this repo, including contributor-maintenance sessions. | A contributor-mode bootstrap request exposed that the existing wording still gated Skill 063 on “operator-facing” work, so an agent could correctly load `AGENTS.md` and the startup docs yet still skip project bootstrap. The intended policy is stricter: project-context bootstrap is part of startup itself, not a later conditional branch. | Future agents should load Skill 063 immediately after the required startup docs in every new session, resolve or create project context before any further task work, and treat contributor sessions as in-scope for the same startup bootstrap behavior. |
| 2026-03-14 | Adopt `docs/projects/` as the canonical durable project-context store and add Skill 063 as the session-start project bootstrap owner, with project-local `.env`/`.envs` files for environment-scoped values and a gitignored `.soavirt/projects/<slug>/secrets.env` default for approved sensitive persistence. | The new bootstrap workflow needed one durable, repo-local home for reusable project facts that future agents can match and load without abusing transient `work/` artifacts or bloating startup context. Separating stable project summaries, environment-file references, and optional sensitive storage preserves the existing canonical-source and progressive-loading rules while making cross-session project reuse practical. | Future agents should run project bootstrap at session start, should resolve context in the order `docs/projects/index.yaml -> project.yaml -> referenced environment/reference files`, should persist durable non-sensitive project facts under `docs/projects/`, and should treat sensitive value persistence as an explicit user-approved branch that defaults to a gitignored workspace-local secrets file rather than tracked docs. |
| 2026-03-14 | Make Skill 058 inherit autonomous posture from Skill 033 and bound inherited-autonomous remediation to one initial synthesis attempt plus at most one evidence-driven correction per target, with unresolved targets returned as one aggregated blocker set to Skill 033. | Contributor review of an autonomous Parabank broad-authoring run showed that Skill 033 had already been hardened to true no-interrupt autonomy, but Skill 058 still reopened an explicit remediation approval gate and had no clear retry ceiling if that gate were simply removed. The safer contract is caller-posture inheritance plus bounded remediation and caller-owned interruption UX rather than either repeated confirmation prompts or open-ended retry loops. | Future agents should let Skill 058 keep moving during autonomous Skill 033 runs using only the allowed evidence ladder, should spend the second attempt only on evidence-driven correction, should stop after two attempts per target in the same pass, and should hand any unresolved targets back to Skill 033 in one aggregated blocker packet that explains the target, the last attempts, and the best next candidate or missing user input. |
| 2026-03-14 | Reduce repeated rule prose by keeping one canonical owner per rule, retaining thin local boundary notes only where they prevent handoff drift, and removing low-value template/leaf boilerplate. | Workspace review found three repetition classes that needed different treatment: canonical policy duplicated across bootstrap/workflow/orchestration layers, protective ownership reminders at router/handoff boundaries, and template-driven or stale boilerplate across many leaf cards. The cleanup goal was to shrink docs without destabilizing execution behavior, so full-rule duplication was removed where canonical owners were already strong, while route-back and phase-ownership notes that still carry execution meaning were intentionally retained. | Future contributors should centralize each reusable rule in one obvious owner, use local echoes only when they materially clarify caller/return ownership or nearby branch behavior, and treat template-driven status/reminder text as contributor-workflow material rather than leaf-card runtime guidance. |
| 2026-03-14 | Treat direct generation leaves under Skill 033 broad-authoring runs as phase owners rather than terminal owners. | Contributor review of an autonomous `help me create tests ...` run showed a real ownership trap: once the user clarified `auto` + `new .tst`, the generation leaf correctly created the host asset, but execution then stopped even though Skill 033 autonomous defaults still required downstream negative, security, and validation phases. The docs already encoded those defaults, but did not explicitly say that resolving the generation branch inside a Skill 033 run does not end Skill 033 ownership. | Future agents should use Skills 022-025 only as happy-path generation phases when a run entered through Skill 033 broad authoring, should return to Skill 033 after generation for readiness stabilization, negatives, security, and validation unless the user explicitly narrowed scope to generation-only, and should report `partial`/`blocked` rather than silently ending at generation when later phases cannot proceed. |
| 2026-03-14 | Standardize preferred wording for Skill 033 and Skill 056 mandatory intake questions while preserving unscripted handling for selected blocker-only follow-ups. | Contributor UX review showed that the skills had strong sequencing guidance but still left some mandatory questions too open to agent wording drift, especially around posture, routing, negative/security toggles, subset selection, payload strategy, and the one-client branch. At the same time, some blocker-only questions still benefit from agent discretion because their wording depends more heavily on the exact missing information. | Future agents should use the preferred wording encoded in Skills 033 and 056 for the main mandatory intake questions, should always present operation lists before asking all-versus-subset or one-operation selection questions, and may continue to phrase certain blocker-specific follow-ups dynamically where the card explicitly leaves them unscripted. |
| 2026-03-14 | Redefine Skill 033 autonomous mode as a true no-interrupt execution path, and align Skill 056 with the same posture semantics. | Contributor review of the latest Skill 033 UX clarified that users choosing autonomous mode want the agent to take a service definition or endpoint anchor and go to work, not stop at an approval artifact or keep a separate payload-fill opt-in. The prior wording treated autonomy more like a lighter interactive planning mode, which did not match the intended user experience. | Future agents should treat Skill 033 autonomous mode as blocker-only intake followed by uninterrupted execution, should default payload fill on in that posture, should reserve the Skill 033 approval artifact for interactive mode only, should defer DB comparison to an explicit earlier request or a post-completion follow-up, and should let Skill 056 inherit the same autonomous posture including default creation of a minimal host `.tst` when no existing target was supplied. |
| 2026-03-13 | Expand Skill 033's encoded standard-negative candidate families while keeping negative planning dynamically reasoned per operation. | Contributor review clarified that the current card already intends dynamic negative-family selection, but its example family set was narrow enough that future agents might under-consider useful protocol/schema/state negatives. Encoding a broader candidate set gives agents more to reason from without turning negative planning into a static exhaustive checklist. | Future agents should continue deriving negative tests from the contract, request shape, and observed service behavior, but may now also consider families such as invalid HTTP method, unsupported media type, unacceptable `Accept` negotiation, null/empty-value violations, length/cardinality issues, malformed or unsupported body representation, unexpected extra fields, and conflict-style negatives when those mutations are plausible for the selected operation. |
| 2026-03-13 | Exclude copied Skill 033 security suites from interim execution passes used for calibration, traffic inspection, diagnostics, and validation-design evidence. | Contributor feedback clarified that Skill 033 security branches are created by copying an already verified-ready happy-path client and attaching a Penetration Testing Tool, while Skill 057 already skips normal validation enrichment for those branches. Running those security suites during internal workflow executions adds avoidable delay without improving the happy-path/standard-negative evidence that the workflow actually needs. | Future agents should keep interim Skill 033 executions scoped to the happy-path and standard-negative slices that need runtime evidence, should not re-run copied security branches just to reconfirm the copied request, and should reserve security-suite execution for explicit security-specific follow-up or an explicit user-approved end-of-workflow full `.tst` run. |
| 2026-03-13 | Treat empty REST Client response payloads as a validation-enrichment no-tool exception, leaving expected response codes as the only validation. | Contributor feedback clarified that when the observed response payload is empty, adding validator/assertor/diff tools does not improve coverage and only creates noise. In this case the meaningful validation is already represented by the REST Client expected response code itself, whether that remains the happy-path default or has been calibrated for a negative test. | Future agents should skip chained validation tools for REST Client targets whose observed response payload is empty, should preserve the happy-path expected `200` behavior or the calibrated negative expected status as the validation, and should surface this as an explicit no-tool exception in the validation plan rather than treating it as an omission. |
| 2026-03-13 | Prefer one combined Skill 057 validation-enrichment pass for ready happy-path and standard-negative targets, instead of separate user-visible approval phases by default. | Contributor review of a recent Skill 033 run found the underlying validation behavior acceptable, but the agent serialized happy-path and standard-negative enrichment into two user-visible phases even though both slices were already in scope and no blocker required the split. The current cards distinguished happy-path vs negative policy correctly, but left too much room for an avoidable extra wait-and-approve cycle. | Future agents should hand ready happy-path and standard-negative slices into one combined Skill 057 plan/approval/execution pass by default, and should split them into separate phases only when readiness divergence, a true blocker, or explicit user instruction requires it. |
| 2026-03-13 | Make detail-enabled execution (`general.showdetails=true`) the default for traffic-observation and focused diagnostics branches, and prefer XML-report fallback over automatic rerun when viewer payloads remain sparse. | Contributor review of repeated "empty traffic viewer" analysis showed the skills were using the correct `/traffic` endpoints but were under-specifying execution-time detail capture. The missing contract was not a new lookup path or a rerun policy; it was a stronger default that any traffic-evidence branch should request detail-rich execution up front, then fall back to decoded XML traffic only if the detail-enabled viewer payload is still sparse. | Future agents should include `general.showdetails=true` by default whenever execution traffic is needed for diagnostics, payload inspection, or validation design, should treat suite->viewer traffic lookup as the canonical API path, and should fail over to XML-report `TrafficData` without inserting an automatic rerun step once a detail-enabled run has already been performed. |
| 2026-03-13 | Make Skill 033 use template-backed approval/completion artifacts, split interactive security intake from standard negatives, and default autonomous runs to standard negatives plus security with always-on validation follow-through. | Contributor review of Skill 033 showed that prose-only output expectations and a bundled negative/security intake model still left too much room for execution drift. Moving the approval/completion UX into templates, adopting grouped `R<n>` review items, keeping security separate in interactive mode but default-on in autonomous mode, and making negative coverage family-based rather than quota-driven gives future agents a clearer execution contract. | Future agents should build Skill 033 approval artifacts from the compact preamble plus Sections `A-E`, use concise completion summaries, treat five negative families as a ceiling rather than a target, ask security as a separate interactive decision, and send security branches through the new Penetration Testing Tool workflow without extra validator/assertor/diff chaining. |
| 2026-03-13 | Tighten direct generation routing so Skills 022-025 are selected only when prompts explicitly request creating a new `.tst` from a service definition, while help-style or outcome-level prompts default to Skill 033 intake. | The latest runtime session showed a realistic ambiguity where a prompt containing an OpenAPI URL (`help me create tests ...`) was concrete enough for direct generation but still semantically underspecified at the intent level. Routing needed a stronger boundary that disambiguates mechanism-specific requests from outcome-level requests in a way that matches natural user phrasing. | Reduces premature bypass of Skill 033 for OpenAPI/WSDL/RAML/XSD authoring, makes direct generation selection more intentional and explicit, and applies one consistent routing boundary across all `.tst` service-definition generation cards rather than only OpenAPI. |
| 2026-03-13 | Remove Change Advisor's redundant intake-summary confirmation gate, and make constrained REST refactor completion stop at structural/persisted-state verification instead of delegated runtime execution. | Contributor review of the latest `ConstrainedREST.tst` Change Advisor run showed two forms of execution drift that a future agent could reproduce from the current docs alone: Skill 061 still required a separate intake-summary confirmation even after the user had already resolved the required `.tst` and OpenAPI anchors, and the delegated `061 -> 060 -> 059` write path still inherited strong focused-execution / traffic-verification wording from the constrained REST lifecycle cards even though the top-level orchestration was intended to stop at structural completion and leave runtime execution as a follow-up suggestion. Tightening both the orchestration card and the delegated leaf/lifecycle wording resolves the user-visible friction and removes the strongest pressure toward post-refactor execution drift. | Future agents should move directly from resolved inputs into read-only Change Advisor analysis, use the approval artifact as the next user-facing checkpoint, stop the bulk refactor workflow at structural/persisted-state verification of the copied `.tst`, and present runtime execution as a follow-up action rather than silently performing it. |
| 2026-03-13 | Remove Change Advisor's subset-selection intake for exact-match source-spec slices, and harden shared discovery rules to require immediate-child exact-name lookup plus response-collection normalization. | Contributor review of the `ConstrainedREST.tst` refactor run showed two issues that were easy for future agents to reproduce: `skill-061` still carried an early-planning artifact that invited subset selection even though the intended bulk workflow should analyze all constrained REST Clients whose binding exactly matches the confirmed source OpenAPI, and runtime discovery could still miss visible `.tst` files when execution assumed a fixed collection key (such as `files`) instead of inspecting the actual top-level response field returned by `descendants/files`. Tightening both together preserves the intended bulk-slice semantics and closes a concrete discovery miss that the existing high-level likely-root guidance did not fully prevent. | Prevents future Change Advisor runs from reopening an unnecessary subset-choice branch, makes the full exact-match constrained-client slice the deterministic default for bulk refactor analysis, and reduces false \"file not found\" misses by forcing fail-closed response-shape handling in shared server discovery. |
| 2026-03-13 | Add README guidance recommending high reasoning for orchestration-heavy workflows and medium reasoning for narrower atomic tasks. | Recent contributor review showed that workflow compliance failures on composite cards are often not about missing cards, but about using too little reasoning depth for tasks that require strict template adherence, fail-closed domain completeness, and cross-skill orchestration. A short operator-facing README note makes this expectation visible without turning the repo into model-specific product documentation. | Helps users choose reasoning settings that better match task complexity, reduces shallow execution drift on orchestration cards such as Change Advisor, and preserves simpler/default settings for atomic tasks where higher reasoning is unnecessary. |
| 2026-03-13 | Make SOAVirt base-URL solicitation a last resort by requiring repo-local cache/reference discovery before prompting the user. | Runtime behavior showed that agents could read `AGENTS.md`, notice an unset `SOAVIRT_BASE_URL`, and ask the user unnecessarily even though the repo already contained a local cached SOAVirt OpenAPI reference and README guidance sufficient to derive a plausible base URL candidate. Tightening both `AGENTS.md` and the canonical runtime workflow closes that bootstrap gap without weakening the later read-probe confirmation gate. | Reduces unnecessary base-URL prompting, makes startup behavior more self-sufficient when one unambiguous local cache/reference exists, and keeps user solicitation reserved for truly missing, ambiguous, or unconfirmed SOAVirt endpoint cases. |
| 2026-03-13 | Make Change Advisor's request-body review semantics deterministic: `NOT_APPLICABLE` only when both source and target operations have no body, target-only fields must surface structured proposal/omission review items, and incomplete body migration keeps the run at `partial` rather than full mechanical success. | The first hardening pass closed the largest omission, but a second contributor review showed future agents could still comply loosely while remaining fuzzy about target-only optional-vs-required fields, overusing `NOT_APPLICABLE`, or overstating completion when body rewrites were not actually applied and semantically verified. Tightening the state rules, review-item shape, and completion semantics closes those remaining ambiguity channels without introducing a large new architecture. | Gives future agents a more deterministic request-body decision model, makes grouped review packets more actionable by carrying concrete field-path/value/omission data, and prevents completion artifacts from overstating success when body-bearing client migration remains partial. |
| 2026-03-13 | Make Change Advisor fail closed on omitted request-body analysis for body-bearing constrained REST Clients, and require proposal-oriented review for target-only request fields when a schema-conformant candidate can be synthesized. | A runtime miss on `/billpay - POST` showed that exact path+method matching plus reference/base-URL rewrites can tempt execution to skip the mandatory request-body mapping branch, leaving a copied `.tst` structurally readable but semantically stale. The same miss clarified that target-only request fields should not disappear into silent omission when the agent can at least propose a generated candidate value or an explicit omission decision for user review. | Prevents future agents from treating body-bearing clients as `AUTO` before explicit body analysis is complete, keeps new-field mapping visible in grouped review, and improves approval artifacts by surfacing generated candidate values or omission proposals instead of hiding unmapped target fields. |
| 2026-03-12 | Codify post-mutation nested-target re-resolution as a shared introspection rule: after container-level copy/generation, nested ids are treated as unstable unless the runtime/API explicitly proves preservation, so targeting must restart from the new container root and narrow by structural path context, type, local identity, and only finally source-tree order. | The first Change Advisor runtime run exposed the problem most clearly during copied `.tst` targeting, but the underlying issue is broader than one orchestration card: any workflow that copies or generates a container asset can otherwise be tempted to reuse stale nested ids or over-trust one broad descendants query. This belongs more naturally in the shared targeting/discovery layer than as repeated one-off local rules. | Gives future copy/generation workflows one reusable fail-closed nested-targeting rule, reduces repeated local wording drift, and lets orchestration cards like Skill 061 reference a shared targeting principle instead of inventing bespoke copied-object resolution semantics. |
| 2026-03-12 | For bulk OpenAPI refactor writes, require constrained `Form JSON` body changes to route through Skill 059's REST Client normalization path, and require copied nested tool ids to be re-resolved from the copied `.tst` root using stable path context instead of assumed source ids. | The first Change Advisor runtime run showed two execution-level temptations that were not hard-stopped strongly enough by the current wording: downloaded YAML made it too easy to momentarily consider direct constrained body-tree edits even though Skill 059 already validated the safer `GET/PUT/GET` normalization path, and copied nested `restClient` ids were not documented with a deterministic re-resolution procedure after whole-file copy. Both issues were narrow enough to harden with targeted card wording rather than a larger workspace refactor. | Reduces future agent drift during copied-target writes, preserves Skill 059 as the canonical constrained JSON body-write mechanism, and gives Skill 061 a deterministic copied-client targeting rule without over-expanding the current architecture. |
| 2026-03-12 | Make Change Advisor a mandatory mutate-on-copy-after-approval workflow after the analysis artifact, add an explicit approval-stage base URL policy choice, and strengthen the approval tree to keep nearby non-target executable context visible. | The first end-to-end runtime execution exposed three contributor-facing mismatches: the card still asked for a stale `planning-only` vs `mutate-on-copy-after-approval` choice, the approval-tree rendering left out useful same-scenario context such as a DB Tool even though the user needed that orientation, and base URL handling remained too implicit even when the target OpenAPI advertised a different server/base URL. Tightening all three together keeps the approval artifact aligned with the intended workflow semantics instead of leaving mode selection, runtime target selection, or local subtree context to ad hoc execution behavior. | Removes an unnecessary intake branch, makes base URL changes explicit and approval-bound, improves approval-artifact orientation for future users, and reduces the chance that future agents will silently preserve or silently change runtime targets during bulk OpenAPI refactor work. |
| 2026-03-12 | Make Change Advisor's initial approval artifact and final completion artifact template-owned output contracts, while keeping the grouped review packet embedded in the initial approval artifact and leaving reduced follow-up review packets as orchestration responses. | The active plan already leaned toward one richer post-analysis artifact that combines the structural tree, grouped review section, and approval question, not two separate first responses. Extracting the stable output shapes into templates makes presentation iteration easier and aligns Change Advisor with existing template-backed output contracts such as Skills 007 and 052, while keeping the review-loop semantics in the orchestration card where they belong. | Gives Change Advisor cleaner separation between workflow semantics and presentation contracts, preserves the intended \"analysis artifact first, reduced follow-up packets later\" interaction model, and establishes reusable template ownership for future refinement of approval/completion presentation without reopening core orchestration logic. |
| 2026-03-12 | Keep the top-level bulk OpenAPI refactor owner as a composite orchestration card named `Change Advisor`, with grouped post-analysis review and copied-target-only writes delegated through the one-client Skill 060 leaf. | Once Skill 060 was detailed enough to own one-client constrained REST Client source-to-target reasoning, the remaining missing piece was a top-level owner for intake, whole-file inventory, grouped review, copied-target sequencing, and final completion reporting. Letting the top-level card absorb one-client semantics directly would blur the composite-vs-leaf boundary and make future refinement harder, while asking the user one ambiguity at a time during analysis would create avoidable review churn. | Establishes a stable operator-facing route for bulk OpenAPI refactor work on existing `.tst` assets, keeps grouped review and copied-target write authorization centralized, and preserves Skill 060 as the reusable lower-layer leaf for one-client refactor execution. |
| 2026-03-12 | Author skill cards for future agents without privileged conversational context, and require explicit wording when execution would otherwise depend on implied author knowledge. | Strengthening the new single-client OpenAPI refactor leaf exposed a broader authoring lesson: cards can read “correct” to the contributor who just designed them while still being underspecified for a later agent that did not participate in the planning conversation. The recurring failure mode is implicit ownership, evidence, review, and fail-closed logic that exists in session memory but not in the card itself. | Adds a reusable contributor rule to the skill-authoring workflow, improves transferability of new cards such as Skill 060, and gives future authoring/review passes a concrete self-check for catching hidden context dependencies before they become execution drift. |
| 2026-03-12 | Introduce a new lower-layer client-tools leaf, Skill 060, to own one-client constrained REST Client OpenAPI refactor work beneath future Change Advisor orchestration. | Planning for bulk OpenAPI version-to-version refactoring clarified that the missing architectural piece is not more broad orchestration first, but a bounded reusable leaf that owns exactly one constrained REST Client subtree's source-to-target mapping, downstream JSON-family repair intent, and client-slice rollback behavior. Existing lifecycle cards such as Skills 010/028/029/031 still provide useful tool-family semantics, but forcing a repo-wide lifecycle-vs-action-contract refactor before this leaf would expand scope too far and delay the core capability. | Establishes a leaf-first implementation path for Change Advisor, gives future agents a reusable one-client refactor owner adjacent to Skill 059, and preserves broader validation/data-exchange contract cleanup as future backlog work rather than a prerequisite. |
| 2026-03-11 | Broaden Skill 059 to own the validated v1 lifecycle for one constrained REST Client, including shell-first creation and a two-phase JSON body-bearing create rule. | Live contributor research on a disposable constrained-rest playground showed a fresh plain REST Client shell can be minimally promoted into a swagger-backed constrained client when operation binding is treated as OpenAPI path plus HTTP method, not `operationId`. The same research also showed same-spec peers are useful only as style exemplars for schema/base-url token reuse, not as mandatory seeds: when no peer exists, fresh constrained creation still works from explicit literals or caller-supplied variable tokens. For JSON body-bearing operations, the reliable path was a second-phase REST Client `GET/PUT/GET` that set schema-valid JSON literal text and let the server normalize the persisted constrained `formJson` model. | Makes Skill 059 the v1 single constrained REST Client lifecycle owner, lets future agents create constrained clients from scratch without requiring an existing constrained peer, codifies path-plus-method binding and same-spec style reuse as deterministic guidance, and keeps broader orchestration and non-JSON constrained body modes out of scope. |
| 2026-03-10 | Split platform file lifecycle guidance into a family card plus atomic copy/rename/delete leaves, and repurpose Skill 006 into a rollback-preserving local YAML edit composite. | Contributor cleanup review showed that Skills 003-005 still mapped well to distinct endpoint/intent pairs, but duplicated shared lifecycle guidance and were obscured by Skill 003’s copy-vs-rename naming drift. The same review showed Skill 006 had little real reuse in its generic form, but future local YAML fallback branches would benefit from a reusable rollback-preserving workflow rather than repeated copy/download/upload/restore wording inside leaf cards. | Keeps copy, rename, and delete as clean atomic owners; reduces duplicated lifecycle prose through a family card; and preserves Skill 006 only because it now serves real YAML-fallback write branches such as constrained REST edits and structural YAML fallback workflows. |
| 2026-03-10 | Treat `/TestAssets`, `/VirtualAssets`, and `/ProvisioningAssets` as canonical server root directories for shared file discovery/transfer, and prefer likely-root-first resolution before broader recursive scans. | Contributor review of a Skill 007 summarization run showed unnecessary discovery churn because the stable server root layout was only implied in Skills 001/002 rather than expressed as an operational heuristic. The same review clarified that Provisioning Assets live under `/ProvisioningAssets` and commonly use the `.pvn` extension. | Makes future agents more efficient and predictable during target resolution, reduces invalid or over-broad recursive discovery attempts, and documents `/ProvisioningAssets` + `.pvn` as first-class shared-file workflow inputs alongside `/TestAssets`/`.tst` and `/VirtualAssets`/`.pva`. |
| 2026-03-10 | Preserve approved per-target validation plans through leaf-skill execution, and classify validation media type from the observed payload/body rather than trusting response headers alone. | Contributor validation of `ValidationEnrichmentTest.tst` showed that request-readiness remediation and validation planning behaved well, but execution later drifted by collapsing a mixed JSON/XML/text plan into Diff Tool-only coverage, omitting approved JSON/XML validators, and over-trusting a misleading `Content-Type` header on a plain-text response. The existing orchestration and leaf-skill docs did not make approved-plan authority or header-vs-body evidence priority explicit enough. | Makes approved per-target execution plans binding across orchestration-to-leaf handoff, prevents silent validator omission or family substitution without re-approval, and hardens validation-family routing against buggy server headers by preferring observed payload/parser reality over response headers. |
| 2026-03-10 | Restrict Skill 058 candidate-value sourcing to user-confirmed context, contract hints, and same-`.tst` evidence by default, and require best-guess proposals instead of autonomous live-service probing when evidence is insufficient. | Runtime remediation on `ValidationEnrichmentTest.tst` showed that autonomous business-data discovery via direct service probing was slower, less predictable, and bypassed the intended user-in-the-loop remediation model. Contributor review clarified that reuse may span any tests or datasources within the same `.tst`, but not other `.tst` files, and that weak-evidence cases should move quickly to labeled best-guess proposals for user correction rather than trial-and-error probing. | Makes request-readiness remediation fail closed on sourcing, keeps candidate values local to the active `.tst`/contract/user context unless the user explicitly opts into autonomous probing, and reinforces that remediation writes must still follow explicit approval of the proposed values. |
| 2026-03-10 | Treat REST Client `GET/PUT/GET` as the reliable same-operation path for constrained `Form JSON` request payload editing, with the server trusted to normalize valid schema-conformant JSON into persisted constrained payload state. | Live testing on `ValidationEnrichmentTest.tst` showed that editing sparse/fresh constrained payloads through the REST Client API caused the server to expand and persist authoritative `formJson` plus `MessagingClient_LiteralMessage`, and focused traffic sent the edited JSON on the wire. The probes covered a sparse object payload (`/billpay - POST (fresh)`) and a second constrained payload with enums, booleans, optional strings, and an array (`/v1/demoAdmin/preferences - PUT (fresh)`). | Shifts Skill 059 from a YAML-primary constrained-payload posture to an API-first constrained JSON payload workflow, lets future agents rely on request-schema-driven JSON construction plus server normalization for same-operation constrained payload remediation, and keeps YAML focused on same-operation resource/config surfaces rather than hand-built payload-tree synthesis. |
| 2026-03-10 | Extend Skill 059 to validated same-operation constrained `Form JSON` body-value edits with synchronized `formJson` + literal surfaces, and make Skill 057 state explicitly that remediation approval never authorizes validation writes. | Live contributor testing on `ValidationEnrichmentTest.tst` showed that editing `MessagingClient_LiteralMessage` alone persisted in YAML but did not change the runtime request body; the authoritative constrained payload change required the schema-derived `formJson` value tree and the display literal to move together. The same review also confirmed that Skill 057's prior wording left too much room to treat remediation approval as implicit validation approval. A follow-on tightening pass showed the payload branch also needed a clearer fail-closed boundary: agents may edit only already-present payload-tree descendants and must not synthesize new constrained payload structure from contract guesses. | This captured the earlier same-day YAML-centric payload branch, which was later superseded for constrained JSON payload construction by the API-first REST Client `GET/PUT/GET` normalization decision above; the remediation-to-validation approval boundary remains current. |
| 2026-03-10 | Add a Level 1 constrained REST Client YAML fallback skill, treat `resourceMethod.resourceId` as a high-risk non-goal, and defer operation-retargeting / OpenAPI-migration orchestration. | Contributor research on an existing constrained REST Client showed the server API readback does not expose the persisted constrained parameter model, but YAML fallback safely supported same-operation path/query value edits and semantically equivalent base/schema reference literalization. The same research also showed isolated `resourceMethod.resourceId` edits can break execution, while `router.HTTPClient_Endpoint` and `literalPath` were not runtime-authoritative in the same probes. | Establishes one narrow, reusable skill for safe same-operation constrained-client edits, keeps Skill 020 focused on None mode, gives Skill 058 a specialist fallback for constrained remediation, and records the broader v1-to-v2 migration/orchestration idea as future backlog work rather than over-claiming current support. |
| 2026-03-10 | Make the Skill 057 -> Skill 058 handoff fail closed for underconfigured slices, and require Skill 058 to own a confirmed per-target remediation plan before returning. | Runtime behavior exposed a handoff ambiguity: Skill 057 could inspect response/result characteristics early enough that an agent might start reasoning about Diff Tool or other validation choices before the remediation workflow had actually walked the user through the missing configuration. Skill 058 also implied a confirmation gate in prompts/safety text, but did not normatively require a remediation-plan summary or explicit return states. | Prevents pre-remediation outputs from being reused as validation-design evidence for underconfigured slices, makes Skill 058 explicitly own the remediation conversation and confirmation gate, and allows only verified-ready post-remediation slices to return to validation enrichment. |
| 2026-03-10 | Keep XML Validator scoped to API-client response outputs, require explicit confirmation for inferred validator definition sources, and make Diff Tool wording output-generic for semantic DB results. | The broader consistency scan found one true boundary bug and two wording drifts after the orchestration refactor: Skill 057 still implied XML Validator could attach to DB Tool resultsets, validator leaves were looser than the new schema-source confirmation posture for env-var or inferred definitions, and Skill 031 still read too response-centric even though Diff Tool may compare semantic DB outputs such as `Results as XML`. | Prevents future agents from routing DB Tool resultsets into the wrong validator family, keeps schema-validation sourcing fail-closed unless the user confirms the exact definition source, and clarifies that Diff Tool can operate on semantic runtime outputs beyond API responses while Skill 020 now points repeated validation-enrichment intent back to Skill 057. |
| 2026-03-10 | Refocus Skill 033 on broad authoring ambiguity, generation-critical pre-write gates, and high-level phase sequencing, while handing generalized post-generation readiness remediation to Skill 058. | After Skills 057 and 058 were introduced, Skill 033 still retained too much overlapping request-materialization/remediation logic. That overlap made the intended specialist boundaries less clear and risked reintroducing competing readiness semantics inside the broad authoring card. | Keeps Skill 033 as the canonical ambiguity resolver and broad plan owner, preserves mandatory `.tst` naming and happy-path payload-approval gates in the authoring flow, and makes the `033 -> 058 -> negatives -> 057` sequencing explicit for future agents. |
| 2026-03-10 | Broaden Skill 057 to own direct DB Tool resultset validation enrichment, and broaden Skill 058 to remediate underconfigured DB Tools with stricter multi-signal readiness heuristics. | Validation enrichment now clearly spans both API-response outputs and DB Tool semantic outputs, but the earlier phase-1 wording still left a gap between "validate DB Tool output itself" and "compare API response to DB". The original readiness heuristics also needed stronger guardrails so legitimate negatives, valid empty DB results, and generic server failures would not be mistaken for incomplete configuration. | Gives DB Tool output validation a stable home under Skill 057, extends fail-closed readiness remediation through Skill 015 when DB Tools are incomplete, and makes underconfiguration detection more conservative by requiring combined evidence rather than one weak clue. |
| 2026-03-10 | Add a dedicated request-readiness remediation orchestration card and wire validation enrichment to hand off there when selected tests are not yet meaningfully configured. | Validation enrichment can now fail closed on live runtime evidence, but without a dedicated remediation workflow the system would either misroute underconfigured tests back into broad authoring or try to attach validation to requests that still contain default/empty inputs. A specialist remediation card is a cleaner fit than overloading Skill 033 or repeatedly forcing multi-test repair through the one-client branch. | Introduces a reusable path for repairing underconfigured REST/SOAP client tests, allows Skill 057 to pause and resume around request-readiness work, and creates a later refactoring opportunity for Skill 033 to hand off multi-test request remediation rather than owning it permanently. |
| 2026-03-10 | Create a dedicated validation-enrichment orchestration card and route direct validation-enrichment prompts there instead of continuing to encode that policy inside Skill 033. | Validation-enrichment intent is now common and specific enough that broad service-test intake is no longer the right owner. Keeping the detailed bundle policy inside Skill 033 made schema-validation defaults, live-response evidence requirements, and response-vs-database behavior too easy to apply inconsistently. | Introduces a reusable direct route for "add validations to these tests" prompts, makes happy-path JSON/XML enrichment default to schema validator plus content validation, keeps negative-test validation conservative by default, and returns Skill 033 to ambiguity-resolution and authoring-scope ownership. |
| 2026-03-10 | Keep Skill 033 as the canonical orchestrator for underspecified service-test prompts, but route repeated clarified one-client workflows through a dedicated branch-specific composite card rather than leaving that branch to ad hoc atomic-skill composition. | The existing routing surfaces still over-concentrated service-test authoring under Skill 033 even though the repo now has richer validated leaves for direct generation and direct REST/SOAP client authoring. A stable routing boundary is needed so future agents do not overuse Skill 033 for already-specific requests or re-invent the same one-client branching logic differently across sessions. | Preserves Skill 033's original ambiguity-resolution purpose, introduces a reusable single-client orchestration path, and clarifies that service definitions may act as planning input for one-client REST/SOAP flows while broad generated coverage remains owned by the generation cards. |
| 2026-03-10 | Treat `referenceExistingEnvironment.file` on suite-level WSDL generation as a validated but non-default branch, not as an equivalent substitute for managed environment normalization. | Runtime validation showed the branch does create referenced environment nodes, but they are added as inactive references while the existing active local environment remains active; generated SOAP Clients still resolved `${ENDPOINT}` from that active local environment at runtime, proven by traffic using an intentionally mutated wrong active-environment endpoint. | Preserves the branch for explicit operator use without letting future agents assume UI-like environment handoff behavior, and keeps managed generation plus merge/delete normalization as the default safe path for existing-`.tst` authoring. |
| 2026-03-10 | Treat suite-level WSDL generation into an existing `.tst` as a structure-layer workflow skill with mandatory environment normalization and target-subtree readback, rather than a raw `POST /v6/suites/testSuites/wsdl` endpoint note. | Runtime validation showed two API behaviors that make a POST-only skill unsafe for future agent reuse: `addNewEnvironment.name=<existing-active-name>` created a new inactive suffixed environment (for example `Local 2`) rather than appending into the active environment, and nested-parent generation could return a root-level response id while the actual generated port suite appeared under the nested target subtree. | Gives future agents one safe owner for existing-`.tst` WSDL generation, prevents leaving `.tst` assets in ambiguous multi-environment states, and codifies collision-aware prefixing plus post-generation environment merge/delete as the default practical behavior. |
| 2026-03-09 | Scope the first SOAP Client lifecycle skill to the API-exposed HTTP request/transport/misc surface, treat WSDL-originated clients as compatible via preservation evidence rather than full constrained-WSDL parity, and register `Response SOAP Envelope` as the canonical semantic chaining parent. | Live SOAP Client probes showed direct `soapClients` endpoint support, validated runnable HTTP/XML behavior, and stable XML child-tool chaining under `Response SOAP Envelope`, but did not expose first-class WSDL/XML Schema resource-mode fields in SOAP Client readback. Copy plus read-merge-write PUT preserved runnable behavior for a WSDL-originated SOAP Client without proving full UI-tab editability. | Enables a useful first-pass SOAP Client skill and SOAP output-parent mapping without over-claiming unsupported WSDL-tab or non-HTTP transport authoring, and gives downstream XML-tool skills a concrete SOAP response parent path for future reuse. |
| 2026-03-08 | Harden validation-family cards with fail-closed runtime media-type gating and explicit datasource-backed assertor binding guidance, plus traffic-shape and API type-literal reference notes. | A live runtime mutation exposed avoidable first-pass errors from inferring validator/assertor family from producer class, assuming root-level traffic payload shapes, guessing asset `type` literals, and modeling datasource-backed expected values as fixed `${column}` strings without tool-level datasource binding. | Makes Skills 010/016/029/030/031 fail closed on observed payload type, improves Skill 012 traffic inspection reliability, clarifies practical API `type` matching in Skill 001, and reduces repeat assertor-binding mistakes when expectations come from datasource columns. |
| 2026-03-08 | Centralize exemplar-lookup efficiency policy in `agent-workflow.md` and reduce per-card duplication by referencing the global rule from skill cards. | Repeated local \"optional exemplar\" wording across cards caused maintenance duplication and did not consistently prevent inefficient workspace scans; one canonical runtime policy is easier to enforce and keep aligned. | Improves consistency and efficiency in future agent sessions, reduces card-level drift, and keeps skill cards focused on operation-specific behavior while inheriting global authoring discipline. |
| 2026-03-08 | Introduce a cross-cutting XPath scalar-normalization skill (Skill 054) and wire databank/assertor cards to use XML text-node normalization with explicit JSON boundary behavior. | Live runtime validation showed XML databank selectors without text-node normalization could propagate element markup into downstream variables (for example URL substitution), while JSON XPath-over-JSON selectors did not require equivalent rewriting. | Centralizes selector-normalization policy in one reusable dependency, reduces repeated XML scalar extraction mistakes across XML Data Bank/XML Assertor flows, and prevents accidental over-application of XML text-node rules to JSON selector semantics. |
| 2026-03-07 | Standardize minimal payload-shape disambiguation sections across assertor/validator/databank skills and add a lightweight usage guard for Diff Tool payload templates. | Recent authoring sessions showed agents can overfit to sample literals or infer incorrect nested payload fields when examples are absent/ambiguous; a minimal shape-only pattern with anti-copy rules is sufficient for reliability without heavy duplication. | Improves first-pass payload correctness for Skills 010/016/019/028/029/030 and clarifies Diff Tool template usage in Skill 031 while keeping examples concise and non-prescriptive. |
| 2026-03-07 | Harmonize client-tool intake around explicit create-mode branching (`configured` default vs explicit `scaffold-only`) for REST Client and DB Tool skills. | Live execution drift showed agents could create tools without soliciting required configured inputs, while users expected deterministic intake behavior matching create intent. | Aligns Skills 015 and 020 with a shared mode-selection contract, conditional required-field gating, and incomplete-result reporting for scaffold creates, reducing unintended scaffold/configured mismatches. |
| 2026-03-07 | For REST Client lifecycle guidance, remove None-mode post-update enforcement and accept observed GET->PUT->GET behavior on constrained REST Clients while keeping API-first boundaries. | Runtime probe on constrained `/billpay - POST` copy retained `resource.type=swagger` after read-merge-write PUT, but REST Client GET does not expose UI-visible OpenAPI URL fields; strict None-mode post-update enforcement no longer reflected observed behavior. | Keeps Skill 020 focused on safe read-merge-write behavior without asserting unsupported normalization, and avoids unnecessary forced conversion logic while constrained-mode limits remain API-visible. |
| 2026-03-07 | Require per-turn intent re-routing before reusing previously selected skill cards. | A live interaction showed that carrying forward prior deep-analysis skill context could cause incorrect card reuse when the next prompt shifted to high-level summarization intent. | Reduces context-carryover misrouting, enforces prompt-local intent normalization, and improves consistency with `skill-index.md` routing registry for intent shifts within the same session. |
| 2026-03-07 | Skill 052 must apply step-attribution boundary before recursive consume inference, and validation-reference precedence must prevent chained assertor/validator variables from being emitted as parent-step `Consumes`. | The existing Skill 052 policy already said validation-only references should stay out of `Consumes`, but it did not make the attribution order explicit enough for parent executable nodes versus nested validation child tools. | Makes Skill 052 harder to misapply in mixed executable-plus-validation step trees, reduces false consume attribution, and clarifies when a variable belongs only under `AppliesValidation` versus both `Consumes` and `AppliesValidation`. |
| 2026-03-07 | Clarify that transient local artifacts (`work/` snapshots, repo files, prior run artifacts) are supplementary evidence only and must not replace API-first runtime asset resolution. | A recent mis-execution showed that the optional continuity note about reviewing transient artifacts could be over-applied during runtime `.tst` analysis, even though the canonical routing/analysis policy already required server-API target resolution followed by YAML download. | Makes the API-first targeting rule more explicit in bootstrap/runtime/skill/contributor docs, reduces the chance of local-artifact-first drift, and preserves `work/` as optional evidence rather than an alternate source of truth for runtime asset identity. |
| 2026-03-07 | Keep skill validation/progress state centralized in `docs/skills/backlog.md`, while skill cards own execution semantics, success criteria, and reusable behavior notes rather than dated status snapshots. | Many cards had accumulated `Current Validation Status`, `Validation Snapshot`, `Initial Validation Plan`, and status-bearing `Reuse Notes`, which duplicated the backlog and blurred the difference between contributor status tracking and reusable execution guidance. | Makes card ownership more consistent, keeps `backlog.md` as the canonical status surface, reduces future drift, and leaves skill cards focused on how to execute a capability safely and correctly. |
| 2026-03-07 | Repurpose `docs/skills/backlog.md` from a historical build-order list into the canonical contributor-facing skill status ledger and short backlog. | The old backlog mixed completed build sequence, active inventory, endpoint details, and speculative future cards, which made it stale and duplicated information already owned by `skill-index.md` and individual cards. | Clarifies that `skill-index.md` is the operator-facing routing surface while `backlog.md` tracks validated/defined/in-progress/deferred status, active priorities, and real coverage gaps for contributors. |
| 2026-03-07 | Clarify Skill 050 as the canonical runtime API capability preflight policy for server-API-mediated branches, not only a pre-write helper. | Skill 050 already owned read-only target resolution, transfer, execution-observation, and mutation profiles, but some global/docs wording understated that broader role. | Aligns agent-workflow.md, AGENTS.md, skill-index.md, and backlog wording with the actual architecture while preserving explicit first-write gating for mutation branches. |
| 2026-03-07 | Split the monolithic workflow guidance into `agent-workflow.md` (runtime policy), `skill-authoring-workflow.md` (contributor skill-maintenance process), and `documentation-sync-workflow.md` (canonical doc/log synchronization). | `agent-workflow.md` had grown into a mixed runtime-governance-authoring document that was hard to read and difficult to trust. Separating runtime policy from contributor maintenance workflow clarifies ownership and makes each document easier to use. | Improves readability and maintainability of the workflow docs, gives contributors clearer starting points, and reduces future drift between runtime rules and skill-system maintenance guidance. |
| 2026-03-07 | Complete the taxonomy-refactor transition by making `docs/skills/skill-index.md` the operator-facing intent router while keeping L0-L4 as a secondary architectural view, and retire the standalone taxonomy proposal document. | The proposal's main goals (dual-view taxonomy, shallow domain folders, intent-oriented discoverability, and reduced routing friction) have now been implemented in the canonical docs. Leaving the proposal as a separate workflow artifact would duplicate guidance and create drift. | Preserves the historical rationale in canonical logs, keeps `skill-index.md` as the live navigation surface, and removes an obsolete transition artifact from `docs/workflow/`. |
| 2026-03-07 | Promote Skill 050 to canonical API capability preflight policy with operation-class profiles and explicit ownership boundaries. | Generic preflight behavior had drifted across cards and could reintroduce trial-and-error probing and policy duplication. | Centralizes compatibility classification/routing in one reusable card, reduces duplicated preflight prose in dependent skills, and preserves endpoint/payload ownership in atomic cards (Skills 001/002/012). |
| 2026-03-07 | Treat Skill 050 capability ledger reuse as an opportunistic transient cache under `work/cache/capability-ledger/<baseurl-key>.json`, with canonical cache key and invalidation rules but without making cache presence a prerequisite for preflight. | Multi-skill compositions and multiple prompts in one ongoing session were at risk of repeating identical checks, increasing latency and noise, but the cache lives under transient `work/` storage and must not become a required dependency before live capability probing can begin. | Avoids redundant preflight probes within the same session when cache data is available, preserves live probing when the cache is absent or unreadable, and keeps the same safe refresh triggers (`baseUrl` change, auth-context change, force-refresh, contradictory runtime evidence). |
| 2026-03-07 | Enforce Skill 050 dependency hygiene for write-capable cards and align backlog wording drift for Skill 052 output model. | Several write-capable cards had implicit or inconsistent preflight dependency declarations, and backlog wording for Skill 052 no longer matched the cleaned template contract. | Improves dependency consistency for mutation/execution cards, strengthens preflight loading reliability, and keeps documentation aligned with current template-driven output behavior. |
| 2026-03-06 | Require loading Skill 012 before any baseline/verification execution or traffic-observation branch, and treat Skill 012 payload schema as canonical for `/v6/testExecutions` requests. | A live run showed avoidable ambiguity when execution started without Skill 012 loaded first, causing malformed `testNames` payload shape, failed execution creation, and misleading downstream `404` follow-ups. | Reduces execution-start ambiguity, prevents post-failure status/results/traffic miscalls, and centralizes execution payload correctness in one canonical skill instead of duplicating schema details across atomic cards. |
| 2026-03-03 | Start with shared REST lifecycle skills before tool-specific skills. | Same list/download/upload functions apply across SOAtest and Virtualize. | Reduces duplication and establishes reusable foundation. |
| 2026-03-03 | Focus product-specific deepening on SOAtest first. | Narrow scope while learning skill authoring process. | Faster feedback loop before expanding to Virtualize. |
| 2026-03-03 | Keep a persistent chat log artifact in repo. | Preserve learning history and implementation context. | Improves onboarding and retrospective review. |
| 2026-03-03 | Organize skills by layers (L0-L4) with strict dependency flow. | Future refactoring will become highly granular; layered cards keep retrieval targeted and composable. | Better context-window control, less duplicated logic, cleaner growth path from file ops to test/tool config refactoring. |
| 2026-03-03 | Keep composite cards orchestration-only and reference atomic cards by dependency. | Prevents drift and duplicated endpoint logic while supporting reusable workflows. | Smaller context slices and easier maintenance as granular refactor skills expand. |
| 2026-03-03 | Standardize `.tst` explanations with a protocol-aware human summary template and explicit REST-vs-YAML evidence levels. | REST API is a subset view, while YAML contains full configuration details; users need clear confidence boundaries. | Improves explanation quality, avoids overclaiming from partial API data, and scales better as Messaging/JMS/MQ/GraphQL coverage grows. |
| 2026-03-03 | Default summaries should prioritize what the TST is testing, active environment, and how tests are organized; protocol matrix is optional. | Users need actionable understanding first; deep transport detail is situational. | More useful default explanations while preserving a path for advanced protocol-specific analysis. |
| 2026-03-03 | Keep default summaries concise and descriptive, without built-in recommendations/opinions unless requested. | Users primarily want understanding of behavior and organization before asking for improvements. | Cleaner summaries with lower noise and better alignment to user intent. |
| 2026-03-03 | Use a size-based hierarchy heuristic in summaries (suite-only default for large TSTs). | Large files become unreadable when every test is listed by default. | Better readability with predictable defaults and user override for full detail. |
| 2026-03-03 | Separate reusable skill knowledge from run-specific analysis artifacts. | Target-specific snapshots should not couple general-purpose skills to one teaching file. | Cleaner long-term skill base and clearer context management with `work/runs/...` for transient outputs. |
| 2026-03-03 | Model TST object manipulation as a skill family (umbrella + atomic operation cards + object overlays) rather than one monolithic card. | Copy/paste/move use cases span multiple object types and constraints; decomposition preserves context control and validation quality. | Scalable path to cover suites/tools/data sources/environments/global properties without duplicating endpoint logic. |
| 2026-03-04 | Standardize REST Client JSON payload defaults to `formJSON` + beautified JSON literal, with YAML verification/correction when API readback compacts payload text. | UI expectations for payload formatting/mode are not fully represented in REST API response shape; YAML is the reliable persisted model for these details. | Improves default REST Client quality and reduces repeated manual UI corrections for JSON payload formatting. |
| 2026-03-04 | For client tools, do not explicitly set `Content-Type` request header; define `payload.contentType` and let the client tool emit the header. | SOAtest client tooling manages `Content-Type` automatically from payload configuration; explicit manual header setting can cause drift or conflict. | Improves portability and consistency for REST Client now, and establishes the same default for future SOAP Client, GraphQL Client, and Messaging Client skills. |
| 2026-03-04 | Keep reusable skill cards service-agnostic and avoid codifying target-specific business preconditions as mandatory logic. | Domain/runtime quirks (for example seeded data state or endpoint-specific business constraints) may not generalize across services. | Reduces overfitting, keeps skills reusable, and pushes target-specific details into transient run evidence until a pattern generalizes. |
| 2026-03-04 | Diff Tool workflow requires baseline run-driven mode selection and a required post-config verification run; ignore rules are opt-in by explicit user confirmation after human-readable diff summary. | Real response type must drive diff mode selection; automatic ignore application can hide real regressions if not user-approved. | More reliable diff setup, clearer operator control, and safer handling of volatile fields in regression comparisons. |
| 2026-03-04 | Assertor workflows (JSON/XML) follow baseline run -> observe live response -> configure assertions from user intent -> verification run; failures route to diagnostics instead of ignore-style mutation. | Assertor tools are strict validators and do not support ignored-differences semantics like Diff Tool. | Improves consistency of assertor authoring, keeps assertion intent explicit, and uses diagnostics skills for failure triage without auto-weakening checks. |
| 2026-03-04 | JSON Validator skill defaults to explicit OpenAPI message mapping (direction/path/method/responseCode) and disables auto-detect by default; `work/tmp_*` scripts remain transient helpers only. | Auto-detect can bind to incorrect schema nodes for runtime responses; persistent skill behavior must be codified in skill cards, not inferred from temporary scripts. | More deterministic JSON validation behavior, fewer false schema mismatches, and clearer separation between reusable skill policy and disposable execution helpers. |
| 2026-03-04 | Service-definition-driven generation skills require input-resolution + confidence-gate prompting when source location is missing/ambiguous; multi-step intent capture uses Intake -> Summarize Choices -> Plan/Execute orchestration phases. | Endpoint-level create operations are deterministic but can over-generate without clarified user scope/config intent; explicit gating avoids speculative writes. | Improves safety and intent alignment for generation workflows, and standardizes when to keep logic in atomic cards versus promoting reusable composite orchestration. |
| 2026-03-04 | Store reusable intent-routing orchestration cards in `docs/skills/composite-orchestration/` rather than `platform` to keep endpoint-atomic and cross-domain orchestration concerns separated. | Platform cards should remain endpoint-centric and product-general; orchestration cards manage multi-skill intent branching across domains. | Cleaner information architecture, easier retrieval of intent workflows, and reduced drift between atomic endpoint cards and orchestration logic. |
| 2026-03-04 | First live Skill 033 run hardening: require transient local source snapshot fetch, strict subset pruning after generation, fresh-id and parent-resolution guards, deterministic non-cumulative variant naming, and producer-output-only chaining (never sibling inference). | Live execution exposed failures from stale ids, copy-parent derivation, subset drift, naming drift, and incorrect validation-tool attachment assumptions. | Increases reliability of orchestration execution and aligns implementation behavior with cross-cutting chaining rules (Skills 017/018). |
| 2026-03-04 | Codify subset prune as atomic Skill 047 and treat hardened Skill 033 rerun as in-progress validation stage (24 tests, 7 failures remaining). | Prune behavior depends on validated suite-delete semantics and should be reusable outside one orchestration card; rerun evidence confirms reliability gains with targeted failures still requiring scenario tuning. | Improves composability (atomic + composite separation), preserves proven execution guards, and focuses next iteration on narrowing remaining failing negative/happy variants without regressing orchestration safety. |
| 2026-03-04 | Treat REST Client PUT updates as read-merge-write only (no sparse/name-only PUT), and standardize execution defaults on `soatest.user://Example Configuration` with explicit no-op detection (`testRunCount=0`). | Probe run proved name-only `PUT /v6/tools/restClients` can clear URL/resource fields and silently make generated tests non-executable; execution retries also showed config-key drift causes avoidable trial-and-error. | Prevents accidental URL erasure during orchestration mutations, reduces execution misconfiguration churn, and adds deterministic diagnostics when runs execute files but zero tests. |
| 2026-03-04 | Generalize read-merge-write PUT safety to all pre-configured tool updates (SOAtest + Virtualize policy), not just REST Clients. | Any class-specific tool PUT may behave as replace-semantics in practice; sparse updates can silently drop required config for existing tool instances. | Establishes a cross-cutting default of GET -> mutate -> PUT for existing tools, reducing regression risk across validation/data-exchange/client tool workflows. |
| 2026-03-04 | Normalize core tool cards to lifecycle-first coverage (CRUD + copy where supported) while preserving runtime-validation workflows. | Workflow-only cards made edit/delete/copy capabilities implicit and inconsistent across tool classes, despite shared `/v6/tools` lifecycle patterns. | Improves retrieval and execution consistency across Skills 010/016/019/020/028, and makes mutation safety expectations explicit in each card. |
| 2026-03-04 | Enforce strict response-media-type gating for validator/assertor family selection and route plain-text validation to Diff Tool text mode with baseline-response seeding. | JSON/XML tools were being attached to incompatible response types, causing parser/schema mismatch noise and avoidable failures. | Aligns Skill 033 orchestration and Skills 010/016/029/030/031 with deterministic tool routing: JSON tools for JSON, XML tools for XML, text fallback through Diff Tool. |
| 2026-03-04 | Skill 033 must enforce mandatory pre-write gates for `.tst` naming and write-payload approval (`POST`/`PUT`/`PATCH`). | A live orchestration run skipped explicit file-name and payload approval, causing mismatch with user expectations and avoidable happy-path failures. | Prevents silent auto-naming and unapproved payload generation; agentic contexts may not bypass these gates without explicit user opt-in. |
| 2026-03-04 | Require branch-aware API capability preflight and compatibility-safe fallback routing in orchestration/write-phase workflows (Skill 050). | Live runs exposed server-specific method support differences (for example unsupported endpoint-method combinations) and avoidable retries when assumptions were optimistic. | Reduces `405`-driven churn, makes write-phase routing deterministic, and centralizes compatibility guidance in skills/workflow docs rather than expanding API cache overlays. |
| 2026-03-04 | Enforce ambiguous-write reconciliation before retry, with same-name collision handling as fallback in create flows. | A live `.tst` create incident showed truncated/ambiguous output can lead to blind retries and false `already exists` collisions even when the first write succeeded. | Prevents duplicate-create churn, preserves successful writes by reconciling state first, and standardizes reuse/delete-and-recreate/new-name branching only when collision is confirmed. |
| 2026-03-04 | For validation orchestration, require per-endpoint media-type decisions with runtime-traffic-sourced baselines, and make JSON/XML validator attachment mandatory when selected by the normative table. | A live Skill 033 run produced missing JSON validators and Diff tools configured as text with XML-leaning expected baselines, caused by non-traffic baseline sourcing and cross-endpoint mode bleed. | Hardens Skills 033/031 to prevent validator omission and diff-mode overwrite, and ensures expected baselines come from the same REST Client execution traffic used for media-type detection. |
| 2026-03-06 | Add keyword-based Parasoft intent detection gate for skill routing. | Prompts can be broad and may mix Parasoft and general software intents; cue-based detection helps route only relevant requests into the skill architecture. | Improves skill usage consistency for Parasoft-domain tasks, avoids forcing skill flow on unrelated tasks, and standardizes low-confidence behavior via one clarification question. |
| 2026-03-06 | Harden cross-skill runtime routing and diagnostics defaults: explicit `descendants/files` vs `descendants/assets` targeting, layered preflight with `404` disambiguation, mismatch-safe assertor family gating, regex full-match guidance, and traffic-to-XML fallback evidence flow. | Recent runs showed avoidable inefficiency from endpoint ambiguity, noisy preflight interpretation, media-type/tool mismatches, regex expectation drift, and empty viewer traffic gaps. | Reduces false routing/probing failures, improves first-pass tool selection, makes assertion behavior more predictable, and preserves diagnostic continuity when traffic viewer payloads are sparse. |

## Entry Template

- **Date**:
- **Decision**:
- **Rationale**:
- **Impact**:
